# Mímir requirements — core floors pinned to audited production versions
# (P0-K, v14.2: fastapi/chromadb/cryptography/pydantic/httpx raised to
#  the versions actually running in production since 2026-09; full lockfile
#  is the venv in the release tree — pip freeze there for an exact pin set)
fastapi>=0.141.1
uvicorn[standard]>=0.29.0
httpx>=0.28.1
jinja2>=3.1.0
aiofiles>=24.1.0
# embedding & vector (ChromaDB)
chromadb>=1.5.9
sentence-transformers==3.4.1
# graph & LLM (optional, helps extract)
transformers>=4.47.0
# security/acl
cryptography>=50.0.1
# util
python-multipart>=0.0.4
pydantic>=2.13.5
pydantic-settings>=2.5.0
# test
pytest>=7.4.0