# Mímir v12.1.0 实现计划 (基准评测、全源采集调度与动态 Agent 注册)

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development 或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 实现 Mímir v12.1.0 核心能力升级：建立标准化自动化评测基准套件（Mímir-Eval）、解耦 Agent/Domain 硬编码支持动态注册，并打通 RSS/Web/Vault 全源自动化采集调度流水线。

**架构：** 
1. 建立基于合成基准+真实生产金标的 Mímir-Eval 自动化评测模块（覆盖召回 HitRate@K、MRR、提取精度与 ACL 隔离泄漏率）。
2. 在 `schema.py` 与 `config.py` 中引入动态 Agent 与 Domain 注册机制，优先从 `mimir_config.yaml` 加载并支持运行时安全校验。
3. 扩展采集器与 CDC 调度接口，将全源采集统一挂载至治理调度流程。

**技术栈：** Python 3.11/3.12, SQLite3, FastAPI, Pytest, BGE-M3 (ChromaDB), YAML

---

### 任务 1：动态 Agent 与 Domain 注册解耦 (Dynamic Agent & Domain Registry)

**文件：**
- 修改：`mimir_v8/schema.py`
- 修改：`mimir_v8/config.py`
- 测试：`tests/test_p17_dynamic_registry.py`

- [ ] **步骤 1：编写动态注册失败测试**

```python
# tests/test_p17_dynamic_registry.py
import pytest
from mimir_v8.schema import (
    get_registered_agents,
    get_registered_domains,
    register_agent,
    register_domain,
    validate_agent_id,
    validate_domain,
)

def test_dynamic_agent_registration():
    initial_agents = get_registered_agents()
    assert "mentor" in initial_agents
    
    register_agent("new_analyst")
    assert "new_analyst" in get_registered_agents()
    validate_agent_id("new_analyst")

def test_dynamic_domain_registration():
    initial_domains = get_registered_domains()
    assert "quant" in initial_domains
    
    register_domain("crypto")
    assert "crypto" in get_registered_domains()
    validate_domain("crypto")
```

- [ ] **步骤 2：运行测试验证失败**

运行：`python3 -m pytest tests/test_p17_dynamic_registry.py -v`
预期：FAIL，报错 "cannot import name 'get_registered_agents'"

- [ ] **步骤 3：在 schema.py 中实现动态注册函数与安全校验**

在 `mimir_v8/schema.py` 中将静态 `AGENT_IDS` 和 `DOMAINS` 转为可动态扩展集合，并提供注册与查询接口。

- [ ] **步骤 4：运行测试验证通过**

运行：`python3 -m pytest tests/test_p17_dynamic_registry.py -v`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add mimir_v8/schema.py mimir_v8/config.py tests/test_p17_dynamic_registry.py
git commit -m "feat(registry): add dynamic agent and domain registration (v12.1.0)"
```

---

### 任务 2：Mímir-Eval 自动化评测套件 (Mímir-Eval Benchmark Suite)

**文件：**
- 创建：`mimir_v8/eval_suite.py`
- 创建：`tests/test_p18_mimir_eval.py`

- [ ] **步骤 1：编写评测指标与计算模块的测试**

```python
# tests/test_p18_mimir_eval.py
from mimir_v8.eval_suite import compute_retrieval_metrics, compute_acl_leak_rate

def test_retrieval_metrics_calculation():
    ground_truth = ["fact_1", "fact_2"]
    retrieved = ["fact_3", "fact_1", "fact_4", "fact_2"]
    
    metrics = compute_retrieval_metrics(ground_truth, retrieved, top_k_list=[1, 3, 5])
    assert metrics["hit_rate@1"] == 0.0
    assert metrics["hit_rate@3"] == 1.0
    assert metrics["mrr"] == 0.5  # First hit is at rank 2 -> 1/2

def test_acl_leak_rate_calculation():
    # 5 queries by quantmaster, 1 retrieved owner_only fact of mentor
    results = [
        {"agent": "quantmaster", "allowed": True},
        {"agent": "quantmaster", "allowed": False}, # leak!
    ]
    leak_rate = compute_acl_leak_rate(results)
    assert leak_rate == 0.5
```

- [ ] **步骤 2：运行测试验证失败**

运行：`python3 -m pytest tests/test_p18_mimir_eval.py -v`
预期：FAIL

- [ ] **步骤 3：编写 eval_suite.py 实现指标计算与基准运行器**

实现 `compute_retrieval_metrics`、`compute_acl_leak_rate` 以及离线合成基准打分。

- [ ] **步骤 4：运行测试验证通过**

运行：`python3 -m pytest tests/test_p18_mimir_eval.py -v`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add mimir_v8/eval_suite.py tests/test_p18_mimir_eval.py
git commit -m "feat(eval): add standard Mímir-Eval benchmark evaluation suite"
```

---

### 任务 3：统一全源采集管道调度接口 (Ingestion Pipeline)

**文件：**
- 修改：`mimir_v8/collectors/base.py`
- 修改：`mimir_v8/worker.py`
- 测试：`tests/test_p19_ingestion_pipeline.py`

- [ ] **步骤 1：编写全源采集调度流测试**
- [ ] **步骤 2：运行测试验证失败**
- [ ] **步骤 3：补齐统一采集入口与调度逻辑**
- [ ] **步骤 4：运行测试验证通过**
- [ ] **步骤 5：Commit**

---

### 任务 4：版本自省与全量回归测试验证

- [ ] **步骤 1：更新 `mimir_v8/schema.py` 中的 `MIMIR_VERSION` 为 `"12.1.0"`**
- [ ] **步骤 2：运行全量单元测试与金标评测**
运行：`python3 -m pytest`
- [ ] **步骤 3：Commit & 准备发布**
